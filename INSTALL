diadump installation
====================


Python ``pip`` package is required to install ``diadump``.


From sources
------------

Use the following command line to install ``diadump`` from sources directory (containing setup.py):

    pip install .

or

    python setup.py install


From PyPI
---------

Alternatively you can install ``diadump`` from PyPI:

    pip install diadump


Use `-U` flag for upgrade:

    pip install -U diadump

